import type { ReactNode } from "react";
export type Page = "Inicio"|"Pulso"|"Aprende"|"Practica"|"Mercado"|"Portafolio"|"Perfil";
const navigation:{label:Page;icon:string}[]=[{label:"Inicio",icon:"⌂"},{label:"Aprende",icon:"◇"},{label:"Practica",icon:"△"},{label:"Mercado",icon:"◎"},{label:"Portafolio",icon:"▱"},{label:"Perfil",icon:"○"}];
export function AppShell({page,onNavigate,profile,children}:{page:Page;onNavigate:(page:Page)=>void;profile:{experience:string};children:ReactNode}) { return <div className="app-shell"><aside className="sidebar glass-sidebar"><button className="brand-button" onClick={()=>onNavigate("Inicio")}><img src="/brand/nuvyra-mark.svg" alt=""/><span><b>NUVYRA</b><small>BETA · 0.1</small></span></button><nav aria-label="Navegación principal">{navigation.map(item=><button key={item.label} className={page===item.label?"active":""} onClick={()=>onNavigate(item.label)}><span className="nav-icon">{item.icon}</span>{item.label}{item.label==="Practica"&&<small>SIM</small>}</button>)}</nav><div className="sidebar-context"><p className="micro-label">TU CONTEXTO</p><div><span className="profile-orb">{profile.experience==="En calibración"?"?":profile.experience[0]}</span><span><b>{profile.experience}</b><small>Perfil adaptable</small></span></div><button onClick={()=>onNavigate("Perfil")}>Ver perfil <span>→</span></button></div></aside><div className="app-content"><header className="mobile-header glass-sidebar"><button className="brand-button" onClick={()=>onNavigate("Inicio")}><img src="/brand/nuvyra-mark.svg" alt=""/><b>NUVYRA</b></button><button className="profile-shortcut" onClick={()=>onNavigate("Perfil")}>Perfil</button></header><main>{children}</main><nav className="bottom-nav glass-sidebar">{navigation.slice(0,5).map(item=><button key={item.label} className={page===item.label?"active":""} onClick={()=>onNavigate(item.label)}><span>{item.icon}</span><small>{item.label}</small></button>)}</nav></div></div>; }
